

## Versão integrada

A versão utilizada pelo sistema final está em `src/backend`: Laravel + React/Vite + API MySQL. O fluxo é Portal FIEC -> Login -> Painel -> Fazer Chamado, e os chamados criados são gravados na tabela `chamados` e recarregados no Painel/Inbox.
